name: nsu_audit_mobile
description: NSU Audit Core Mobile App - Graduation audit service for NSU students
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.5.0
  image_picker: ^1.0.7
  http: ^1.2.0
  file_picker: ^8.0.0+1
  flutter_secure_storage: ^9.0.0
  go_router: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
